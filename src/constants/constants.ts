// nav items
export enum NavItems {
    Home = "translate-x-0",
    About = "translate-x-[50px]",
    Awards = "translate-x-[100px]",
    Contact = "translate-x-[150px]"
}

// header content
export enum Titles {
    banner = "Ink your story, wear your journey,",
    highlightBannerHeader = "where art meets skin.",
    recentArt = "Recent Art",
    qoute1 = '"Ink to remind, not to regret."'
}

// buttons content
export enum Buttons {
    Appointment = "Book Appointment"
}

// timers
export enum Timers  {
    time = 2000
}

// contact

export enum Contact {
    number = "+917387776775"
}